{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window. Deliberately minimal: the webview never touches the network, the filesystem or the player. Everything goes through explicit Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
